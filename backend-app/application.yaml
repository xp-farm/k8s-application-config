apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: old-monolith
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/xp-farm/k8s-application-config.git
    targetRevision: main
    path: backend-app
    directory:
      recurse: true
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  # syncPolicy:
  #   automated:
  #     prune: true
      # selfHeal: true
